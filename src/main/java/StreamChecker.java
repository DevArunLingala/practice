public class StreamChecker {
    String[] words;
    public StreamChecker(String[] words) {
        this.words = words;
    }

    public boolean query(char letter) {
        return false;
    }

    private void heapify() {
        int l = this.words.length;
        for (int i = l -1 ; i >= 0; i--) {
            int nodeI = i + 1;
            // check left
           // if ()
        }
    }
}
